# Official SDK Artifacts

Put the official VIVE AI Glasses Android SDK `.aar` or `.jar` files here after downloading the SDK package from HTC/VIVE.

This folder is referenced by Gradle as a local flat directory repository. The SDK binaries themselves are ignored by `.gitignore` so licensed vendor files are not accidentally committed.

